{
  "name": "miniapi-python",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "workspaceFolder": "/workspaces/miniapi",
  "forwardPorts": [8000],
  "postCreateCommand": "python -m pip install -r requirements.txt"
}